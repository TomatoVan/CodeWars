"use strict";

function flattenAndSort(array) {
  // Good luck, brave code warrior!
  console.log(array = [].concat(...array).sort((a, b) => a - b));
  return array = [].concat(...array).sort((a, b) => a - b);
}